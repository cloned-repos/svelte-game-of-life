/// <reference types="@sveltejs/kit" />

/*import { build, files, version } from '$service-worker';

// doesnt work in firefox

build.forEach((f) => console.log('build', f));
files.forEach((f) => console.log('files', f));
console.log('version', version);
*/
