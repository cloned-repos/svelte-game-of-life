// for domain and ranges
export type Interval = {
	from: number;
	to: number;
};

export type Tick = {
	dPoint: number;
	pPoint: number;
	len: number;
};

export type TickLabel = {
	dPoint: number;
	pPoint: number;
	text: string;
};
