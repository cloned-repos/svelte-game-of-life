/*

export default function emitChartResize(
	oldsize: CanvasSize,
	newSize: CanvasSize,
	queue: ChartCommands[]
): false |  {
	if (isCanvasSizeEqual(iState.size, newSize)) {
		return false; // do nothing
	}
	return [
	{ type: CHANGE_SIZE, size: iState.size },
	{ type: CHART_RENDER }
	]
}

*/
